"""security/ — deterministic privilege-control layer for agent tool calls.

Currently exposes :mod:`security.progent_gate`, an argument-level tool-call
policy gate that complements the name/budget checks in
:mod:`services.governance`.
"""
